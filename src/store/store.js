import { createStore } from "vuex";

const store = createStore({
    modules: {
    },
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    /*    plugins: [
            createPersistedState({
                paths: ['checkup'],
            })
        ]*/
});

export default store